{
  "name": "heng-yau-incense",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "build:cloudflare": "node scripts/clean-cloudflare-output.mjs && nuxt generate && node scripts/finalize-cloudflare-output.mjs",
    "check:structured-data": "node scripts/check-product-structured-data.mjs dist/index.html",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "nuxt typecheck"
  },
  "engines": {
    "node": ">=22.16.0"
  },
  "dependencies": {
    "@lucide/vue": "1.44.0",
    "nuxt": "4.3.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vue-tsc": "^3.0.6"
  }
}
